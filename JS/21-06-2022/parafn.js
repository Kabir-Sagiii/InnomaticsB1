function getDetails(name, city, gender, phone) {
  console.log(name, city, gender, phone);
}

getDetails("Kabir", "Delhi", "male", 99999999);

function fnAdd(x, y) {
  var num1 = x;
  var num2 = y;
  var result = num1 + num2;
  console.log(result);
}

fnAdd(100, 300); //400
fnAdd(30, 40); //70
fnAdd(50, 50); //100
